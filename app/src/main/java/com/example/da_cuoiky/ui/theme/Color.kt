package com.example.da_cuoiky.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Palette ─────────────────────────────────────────
// Primary: Amber Orange — table occupied, CTAs, prices
val PrimaryColor = Color(0xFFFF6F00)
val PrimaryVariant = Color(0xFFE65100)

// Secondary: Blue — reserved, info states
val SecondaryColor = Color(0xFF1976D2)

// Semantic colors — match status across all 3 platforms
val SuccessColor = Color(0xFF2E7D32)  // Green: paid, done, confirmed
val WarningColor = Color(0xFFFBC02D)  // Yellow: gold tier, warnings
val ErrorColor = Color(0xFFD32F2F)    // Red: overdue, error, locked

// ── Surface Tokens ────────────────────────────────────────
val BackgroundLight = Color(0xFFFFFFFF)
val SurfaceLight = Color(0xFFF5F5F5)
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)

// ── Misc ──────────────────────────────────────────────────
val ColorWhite = Color(0xFFFFFFFF)
val ColorBlack = Color(0xFF000000)